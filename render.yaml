services:
  - type: web
    name: phantom-protocol
    runtime: node
    rootDir: phantom-relayer-dashboard
    buildCommand: cd backend && npm install
    startCommand: node backend/src/index.js
    envVars:
      - key: RENDER
        value: "true"
      - key: NODE_ENV
        value: production
